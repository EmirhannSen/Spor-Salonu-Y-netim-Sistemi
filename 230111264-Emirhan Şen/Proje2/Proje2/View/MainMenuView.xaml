<Window x:Class="Proje2.View.MainMenuView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Ana Menü"
        Height="700"
        Width="900"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        MouseDown="Window_MouseDown">

    <Border CornerRadius="20" BorderThickness="3">
        <Border.Background>
            <ImageBrush ImageSource="pack://application:,,,/Proje2;component/Imagess/image.jpg" Stretch="UniformToFill"/>
        </Border.Background>

        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#6A0DAD" Offset="0"/>
                <GradientStop Color="#00FFFF" Offset="1"/>
            </LinearGradientBrush>
        </Border.BorderBrush>

        <Grid>
            <!-- Üst pencere butonları -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,10,0">
                <Button Content="—" Width="30" Height="30" FontSize="16" Background="Transparent" Foreground="White" BorderThickness="0" Click="Minimize_Click"/>
                <Button Content="X" Width="30" Height="30" FontSize="16" Background="Transparent" Foreground="White" BorderThickness="0" Click="Close_Click"/>
            </StackPanel>

            <!-- Sol Menü Paneli -->
            <Border Background="#CC1C1C1C" Width="230" CornerRadius="20" HorizontalAlignment="Left" Margin="10">
                <StackPanel Orientation="Vertical">
                    <Grid Height="80" VerticalAlignment="Top" Margin="0,30,0,0">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock x:Name="HosgeldinAdSoyadText" Text="Hoşgeldin" Foreground="White" FontSize="20" FontWeight="Bold" TextAlignment="Center"/>

                        </StackPanel>
                    </Grid>

                    <ListBox x:Name="SideMenu"
                 Background="Transparent"
                 BorderThickness="0"
                 Margin="0,20,0,0"
                 SelectionChanged="SideMenu_SelectionChanged"
                 SelectedIndex="0"
                 ScrollViewer.VerticalScrollBarVisibility="Hidden">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="10" VerticalAlignment="Center">
                                    <Image Source="{Binding Icon}" Width="25" Height="25" Margin="0,0,10,0"/>
                                    <TextBlock Text="{Binding Title}" Foreground="White" FontSize="16"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>

                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Padding" Value="15"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="5">
                                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#66b6d2"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#3C3F45"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                    <Button Width="48" Height="48" Click="CikisYap_Click" Margin="40">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"

BorderThickness="0"
BorderBrush="Transparent"
Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <!-- Triggers -->
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background">
                                            <Setter.Value>
                                                <ImageBrush ImageSource="/Imagess/backs.png"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>

                        <!-- Normal görünüm -->
                        <Button.Background>
                            <ImageBrush ImageSource="/Imagess/back1.png"/>
                        </Button.Background>
                    </Button>


                </StackPanel>
            </Border>

            <!-- İçerik -->
            <ContentControl x:Name="MainContent"
                            Margin="250,30,30,30"
                            Background="#CC1C1C1C"
                            />
        </Grid>
    </Border>
</Window>
