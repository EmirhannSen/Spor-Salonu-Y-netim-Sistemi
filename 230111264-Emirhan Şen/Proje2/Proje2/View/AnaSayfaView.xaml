<UserControl x:Class="Proje2.View.AnaSayfaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="700">

    <Grid Background="Transparent" Margin="10">

        <!-- Saat ve Tarih Kutusu -->
        <Border CornerRadius="12" Background="#AA1E1E1E" Padding="15"
                HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="0,20,10,0" Width="230">
            <Border.Effect>
                <DropShadowEffect Color="#FF1E1E1E" BlurRadius="10" ShadowDepth="2" Opacity="0.9"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                <Viewbox Width="24" Height="24" Margin="0,0,10,0">
                    <TextBlock Text="🕒" Foreground="White"/>
                </Viewbox>
                <StackPanel>
                    <TextBlock x:Name="txtGununTarihi" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                    <TextBlock x:Name="txtGununSaati" FontSize="20" FontWeight="Bold" Foreground="LightSkyBlue"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Başlık -->
        <StackPanel>
            <TextBlock Text="Ana Sayfa" Foreground="Black" HorizontalAlignment="Left" 
                       Width="200" FontSize="26" FontWeight="Bold" Margin="20 25 0 0"/>
        </StackPanel>

        <!-- Kartlar -->
        <Grid Margin="0 100 0 0" HorizontalAlignment="Stretch" VerticalAlignment="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Kartlar -->
            <Border Background="#E6007ACC" Padding="20" Margin="10" CornerRadius="10" Grid.Row="0" Grid.Column="0">
                <StackPanel>
                    <TextBlock Text="Toplam Üye Sayısı" Foreground="White" FontSize="16"/>
                    <TextBlock x:Name="txtToplamUye" FontSize="28" FontWeight="Bold" Foreground="White"/>
                </StackPanel>
            </Border>

            <Border Background="#E628A745" Padding="20" Margin="10" CornerRadius="10" Grid.Row="0" Grid.Column="1">
                <StackPanel>
                    <TextBlock Text="Toplam Eğitmen Sayısı" Foreground="White" FontSize="16"/>
                    <TextBlock x:Name="txtToplamEgitmen" FontSize="28" FontWeight="Bold" Foreground="White"/>
                </StackPanel>
            </Border>

            <Border Background="#E6FFC107" Padding="20" Margin="10" CornerRadius="10" Grid.Row="1" Grid.Column="0">
                <StackPanel>
                    <TextBlock Text="Bu Ayki Yeni Üyeler" Foreground="Black" FontSize="16"/>
                    <TextBlock x:Name="txtBuAykiUye" FontSize="28" FontWeight="Bold" Foreground="Black"/>
                </StackPanel>
            </Border>

            <Border Background="#E6DC3545" Padding="20" Margin="10" CornerRadius="10" Grid.Row="1" Grid.Column="1">
                <StackPanel>
                    <TextBlock Text="Bu Ayki Gelir (₺)" Foreground="White" FontSize="16"/>
                    <TextBlock x:Name="txtBuAykiGelir" FontSize="28" FontWeight="Bold" Foreground="White"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Uyarılar Paneli -->
        <Border Background="#FF1E1E1E" CornerRadius="10" Padding="15" Margin="10,270,10,10" VerticalAlignment="Bottom">
            <StackPanel>
                <TextBlock Text="📌 Duyurular / Uyarılar" Foreground="White" FontWeight="Bold" FontSize="16" Margin="0 0 0 8"/>
                <ListBox x:Name="lstUyarilar"
         Background="Transparent"
         BorderThickness="0"
         IsHitTestVisible="False"
         Focusable="False"
         SelectionMode="Single">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Foreground="LightGray" Margin="0,2">
                <Run Text="• "/>
                <Run Text="{Binding .}" />
                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
